DEFAULT_CONFIG = {
    # whether this program is running is development mode
    "dev": {
        "dev_mode": False,
    },
    # flask server info
    "server": {
        "ip": "localhost",
        "port": 8080,
    }
}
